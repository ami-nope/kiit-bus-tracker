services:
  - type: web
    name: kiit-bus-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 8 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0